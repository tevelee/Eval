# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Interpreter' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Interpreter
  pod "Expression”, :git => "https://github.com/nicklockwood/Expression.git", :branch => "master"

  target 'InterpreterTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'InterpreterPackageDescription' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for InterpreterPackageDescription

end
